export type CallbackEmpty = () => void;

export type CallbackEmpty2 = () => undefined;

export enum FormButtonText {
  ADD_POST = 'Add new post',
  CREATING_POST = 'Creating post...',
  UPDATE_POST = 'Update post',
  UPDATING_POST = 'Updating post...',
}

export enum HelpText {
  ERROR_MESSAGE = 'Required field',
  DEFAULT_AUTHOR = 'unkown',
}
